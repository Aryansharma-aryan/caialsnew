# Comma-separated addresses receive every contact and consultation enquiry.
# Keep this file as a template only; place real values in backend/.env.
ADMIN_RECIPIENTS=team-member-1@example.com,team-member-2@example.com
RESEND_API_KEY=re_your_resend_api_key
MONGODB_URI=mongodb_connection_string
